#include "conduct.h"

__global__ void conduct(Graph G, Int * member, Int num) {

    {
        __S2 = 0;
        __S3 = 0;
        __S4 = 0;
        m = (0~T)(( < )?:);
        if ( == 0)
            return ( == 0)?((float)(0.000000)):FLT_MAX;
        else 
            return ((0~T)) / ;
    }

}
